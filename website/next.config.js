/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Non-optimized images for simplicity; switch to optimized later if needed
  images: { unoptimized: true },
};

module.exports = nextConfig;

